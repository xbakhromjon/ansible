---
# Note: the sshd reload is no longer a handler — it runs as a plain task INSIDE
# the block in tasks/apply.yml. Reason: a handler runs outside the block, after
# it, so rescue could NOT catch a reload failure. Moving the reload inside the
# block lets rollback (rescue) cover a reload failure too.
