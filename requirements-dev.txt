# LOCAL TESTING only (molecule). Not used in production/management, which is
# why it is kept separate from requirements.txt. Install with: make molecule-setup
#
# The Docker driver is required (roles are tested in containers). molecule 6.x
# is compatible with ansible-core 2.16.
molecule>=6,<7
molecule-plugins[docker]>=23,<24
docker>=7
